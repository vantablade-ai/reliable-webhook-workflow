[project]
name = "reliable-webhook-workflow"
version = "1.0.0"
description = "Standalone proof of durable asynchronous webhook processing"
requires-python = ">=3.11"

[tool.ruff]
line-length = 120
target-version = "py311"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
